# Plan / 计划

1. 复核被 `source-lag` 标到的 canonical 页，与新 source 页对齐稳定结论。
   Review the canonical pages flagged by `source-lag` and align their stable conclusions with newer source pages.

2. 刷新 `hot/index/overview/log`，把新的 drift review surface 纳入导航层。
   Refresh `hot/index/overview/log` and integrate the new drift-review surface into the guide layer.

3. 写出 drift review report，并把 report 自身收口到最终稳定状态。
   Write the drift-review report and close the report itself to the final stable state.

4. 用 `drift-review`、`maintain` 和格式检查验证结果。
   Verify the result with `drift-review`, `maintain`, and formatting checks.
