# Plan / 计划

1. 复现并确认 command-generated report 的 index residue。
   Reproduce and confirm the index residue left by command-generated reports.

2. 提取共享的 index registration helper，并接入 `maintain` 与 `drift-review`。
   Extract a shared index-registration helper and wire it into `maintain` and `drift-review`.

3. 补回归测试、文档和验证记录。
   Add regression coverage, docs, and verification records.
