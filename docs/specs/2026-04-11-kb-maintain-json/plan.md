# Plan / 计划

1. 为 `maintain` 提炼统一的 payload builder 与 summary renderer。
   Extract a shared payload builder and summary renderer for `maintain`.

2. 为命令增加 `--json` 参数，并接入 report 元信息。
   Add the `--json` flag and wire in report metadata.

3. 补测试、命令文档与验证。
   Add tests, command docs, and verification.
