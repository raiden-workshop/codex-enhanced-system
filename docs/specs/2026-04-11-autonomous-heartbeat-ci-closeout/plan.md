# Plan / 计划

1. 新增最小 GitHub workflow，让知识库测试与健康检查具备远端执行入口。
   Add the smallest GitHub workflow so the knowledge-base tests and health checks can run remotely.
2. 创建当前线程的 heartbeat automation，并让它默认调用 [$method-forge-autonomous-execution](../../../method-forge/skills/method-forge-autonomous-execution/SKILL.md)。
   Create a heartbeat automation for this thread and make it default to [$method-forge-autonomous-execution](../../../method-forge/skills/method-forge-autonomous-execution/SKILL.md).
3. 运行本地验收命令，确认 repo、workflow 文件和 automation 目标语义都已对齐。
   Run local validation commands to confirm the repo, workflow file, and automation target semantics are aligned.
